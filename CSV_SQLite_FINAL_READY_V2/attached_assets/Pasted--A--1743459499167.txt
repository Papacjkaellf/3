давай сделаем правило для нашего приложения - а именно зададим то что 

столбец A - это всегда Название
столбец B - это всегда Полный адрес (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец C - это всегда улица (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец D - это всегда Город from CSV  (по умолчанию  чекбокс отображения в таблице СНЯТ) 


Для отображения того какой город мы создадим дополнительный столбец в котором будет указан Город который я указал при импорте файла так как в импортируемой таблице он иногда бывает не совсем корректно указан) - соответственно при импорте для каждого файла справа должно быть поле в которое я смогу мануально вписать какой это город - Соответственно Называться этот столбец будет Город*
столбец E - Тип заведения
столбец F - Телефон
столбец G - телефоны (столбец G должен объединяться со столбцом F - а каждый номер должен быть кликабельным для переходя в whatsapp чат и каждый номер должен писаться через запятую) 
столбец H - claimed - по умолчанию  чекбокс отображения в таблице СНЯТ
Столбец I - Просмотров в gMaps (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец J - Средний рейтинг  (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец K - Ссылка в Google  (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец L - Ссылка на Google Maps (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец M - Широта (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец N - Долгота (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец O - Вебсайт 
столбец P - Домен (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец Q - Часы работы (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец R - Картинка  (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец S - CID (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец T - Place ID (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец U - KGMID (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец V - Plus Code (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец W - Ссылка Google Knowledge (по умолчанию  чекбокс отображения в таблице СНЯТ)
столбец X - EMAIL 
Столбец Y - Социальные сети (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец Z - Facebook (кликабельная ссылка - ширина ячейки не шире самого заголовка всегда)
Столбец AA - Instagram (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец AB - Twitter (по умолчанию  чекбокс отображения в таблице СНЯТ)
Столбец AC - Yelp  (по умолчанию  чекбокс отображения в таблице СНЯТ)

соответственно при импорте данных из CSV файла каждый столбец должен быть обработан и его данные должны быть размещены в указанных в правиле ячейках

при первичном импорте должна создаваться MySQL БД со структурой и логикой которая описана в этом правиле

Данная MySQL БД должна иметь название AllTywekCustomersBase

в случае если какой-то файл который будет импортироваться в MySQL БД в соответствии с заданным правилом ориентируясь на первую строку в виде заголовков столбцов

если будут какие-то дополнительные столбцы в импортируемой таблице - то должно появиться предупреждающее сообщение с тем что Импортируемый CSV файл содержит столбцы которых нет в БД - импортировать эти столбцы или нет?

Так же каждый столбец должен иметь возможность регилироваться по ширине ( сейчас данная функция есть - НО она не работает - нужно пофиксить)

